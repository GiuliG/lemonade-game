'use strict'

function Game() {
  var self = this;
  self.gameIsOver = false;
  self.score = 0;
  self.isPause = false;
  self.initialPositionPlayer = {
    x: this.canvasElement.height / 2,
    y: 0
  }

  
}
